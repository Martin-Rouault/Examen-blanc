# URI de connexion MongoDB (local ou MongoDB Atlas)
MONGO_URI=

# Secret utilisé pour signer les tokens JWT
# Générer une clé forte avec :
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=

# Port d'écoute du serveur backend
PORT=5001
